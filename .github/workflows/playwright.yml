name: Playwright Tests

on:
  push:
    branches: [ main, master, workflow_dispatch ]
  pull_request:
    branches: [ main, master ]

jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: lts/*

      - name: Install dependencies
        run: npm ci --force

      - name: Install Playwright Browsers
        run: npx playwright install --with-deps --force

      - name: Create .env file 🔐
        run: |
          echo "STATIC_USER_NAME=Oleh" >> .env
          echo "STATIC_USER_EMAIL=mykhayliv88777@gmail.com" >> .env
          echo "STATIC_USER_PASSWORD=Europe2025$" >> .env
          echo "BASE_URL=https://automationexercise.com" >> .env
          echo "CI=true" >> .env

      - name: Run Playwright tests
        run: npm run pageObjects-chrome

      - uses: actions/upload-artifact@v4
        if: ${{ !cancelled() }}
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30
